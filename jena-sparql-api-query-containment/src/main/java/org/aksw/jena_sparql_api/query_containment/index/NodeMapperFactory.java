package org.aksw.jena_sparql_api.query_containment.index;

import java.util.Map;

import com.google.common.collect.BiMap;

//public interface NodeMapperFactory<A, B, N, R, TCA, TCB, LCA, LCB> {
//	NodeMapper<A, B, BiMap<N, N>, BiMap<N, N>, R> createNodeMapper(TCA treeContextA, TCB treeContextB, Map<A, LCA> nodeToLeafContextA, Map<A, LCB> nodeToLeafContextB);
//}
